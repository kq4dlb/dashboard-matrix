# Validation report — Dashboard Matrix 0.1.0-beta.1

Validated on July 24, 2026.

- 41 automated tests passed
- Python source compilation passed
- Dashboard JavaScript syntax passed
- GitHub Actions YAML parsing passed
- Public cached update-status endpoint passed
- Header installed-version and update-indicator markup passed
- Existing database migration for update-status fields passed
- Stable and prerelease version comparison passed
- Merge version-roll rules passed for beta, patch, minor, major, and stable
- `VERSION`, `version.json`, `app/version.py`, and `pyproject.toml` synchronization passed
- Python wheel build passed using the local build environment
- Wheel contents include the runtime update service, header template, JavaScript, and CSS

The Windows executable, GitHub release, and Raspberry Pi bundle are generated by
the included GitHub Actions workflows on the target runners. They were not
executed inside this Linux validation environment.

## Autostart validation

- Linux and Raspberry Pi installer shell syntax passed.
- The systemd unit declares boot enablement, network ordering, restart behavior, and persistent writable paths.
- Windows installer and management scripts contain at-startup Scheduled Task handling.
- Docker Compose retains the `unless-stopped` restart policy and includes a health check.
- Optional Raspberry Pi kiosk autostart is isolated to the selected desktop user.
- Automated autostart tests pass with the full project suite.
